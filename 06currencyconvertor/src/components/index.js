/* Import all the component here so every time we dont need to call the individual element from the component insted we can selected all the element here and add it to index.js and access index.js to app.jsx */

import InputBox from "./InputBox";
import React from 'react'

export { InputBox }

// 40 min watched!!

